(require "unittest.ta")
(require "oop.ta")

(printTest "Test of OOP")
(printStep "Test of point class public getter")

(defun [createPoint x y]
       [do
       (def [this] [])
       (=> [this] [x] x)
       (=> [this] [y] y)
       (=> [this] [getx] (lambda [self dummy] [(-> self [x])]))
       (=> [this] [gety] (lambda [self dummy] [(-> self [y])]))
       (=> [this] [printx] (lambda [self dummy] [print (-> self [x])]))
       (=> [this] [setx] (lambda [self x-in] [=> self [x] x-in]))
       ;; Methode mit Parametern
       (=> [this] [incrementedY] (lambda [self in1 in2] [+ (-> self [y]) in1 in2]))
       ])
 
(def [a] (createPoint 4 5))

(call [a] [printx] nil)
(assert (call [a] [getx] 9) 4 "Attribute retrieved correctly")
(assert (call [a] [gety] nil) 5 "Attribute retrieved correctly")
(printStepResult)


(printStep "Test of point class setter")
(call [a] [setx] 2)
(call [a] [printx] nil)
(assert (-> [a] [x]) 2 "Attribute retrieved correctly")
(assert (-> [a] [y]) 5 "Attribute retrieved correctly")
(printStepResult)


(printStep "Test of point class method with args")
(def [b] (createPoint 4 5))
(assert (call [b] [incrementedY] 1 2) 8 "Attribute retrieved modified")
(printStepResult)


(printStep "Internal Method call")
(defun [createLine p1-ref p2-ref]
       [do
       (def [this] [])
       (=> [this] [p1] p1-ref)
       (=> [this] [p2] p2-ref)
       
       (=> [this] [deltax] (lambda [self dummy]
                 [- (call (-> self [p1]) [getx] nil) 
                    (call (-> self [p2]) [getx] nil)]))
                    
       (=> [this] [deltax-abs] (lambda [self dummy]
                 [- 0 (call self [deltax] nil)]))])

(def [point1] (createPoint 1 1))
(def [point2] (createPoint 2 2))

(def [line1] (createLine [point1] [point2]))

(assert (call [line1] [deltax-abs] nil) 1 "Internal call correct")
(printStepResult)


(printStep "New operator for creating anonymous objects")
(defun [createLine x1 x2 y1 y2]
       [do
       (def [this] [])
       (=> [this] [p1] (new (createPoint x1 x2)))
       (=> [this] [p2] (new (createPoint y1 y2)))
       
       (=> [this] [deltax] (lambda [self dummy]
                 [- (call (-> self [p1]) [getx] nil) 
                    (call (-> self [p2]) [getx] nil)]))
                    
       (=> [this] [deltax-abs] (lambda [self dummy]
                 [- 0 (call self [deltax] nil)]))])

(def [line1] (createLine 1 1 2 2))

(assert (call [line1] [deltax-abs] nil) 1 "Internal call correct")
(printStepResult)


(printStep "Test creation and manual destruction")
(def [p1] (new (createPoint 5 10)))

(assert (call p1 [getx] nil) 5 "Point created and accessible")

(delete [p1])

(assert (type (eval [p1])) "Error" "Object successfully deleted (Symbol unbound)")
(printStepResult)

(printStep "Test recursive destruction of composite objects")
(defun [createLine-destructible x1 x2 y1 y2]
       [do
       (def [this] [])
       (=> [this] [p1-sym] (new (createPoint x1 x2)))
       (=> [this] [p2-sym] (new (createPoint y1 y2)))
       
       ;; Destruktor Methode für das Line-Objekt
       (=> [this] [destroy] (lambda [self dummy] 
           [do
             (delete (list (-> self [p1-sym])))
             (delete (list (-> self [p2-sym])))
           ]))
       ])

(def [line1] (new (createLine-destructible 0 0 1 1)))

;; 1. Cleanup of child
(call [line1] [destroy] nil)

;; 2. Cleanup of parent
(delete [line1])

(assert (type (eval [line1])) "Error" "Line object successfully deleted")
(printStepResult)

(printStep "Inherited method")
(defun [createPerson name]
  [do
  (def [this] [])
  (=> [this] [name] name)
  (=> [this] [getName] (lambda [self dummy] [(-> self [name])]))
  ])

(defun [createEmployee name wage]
  [do
  (def [this] (createPerson name))
  (=> [this] [wage] wage)
  ])

(def [sascha] (createEmployee "sascha" 6000))

(assert (call [sascha] [getName] nil) "sascha" "Method correctly inherited")
(assert (-> [sascha] [name]) "sascha" "Method correctly inherited")
(printStepResult)


(printStep "Abstract base class unimplemented")
(defun [createPersonBase name]
  [do
  (def [this] [])
  (=> [this] [name] name)
  (=> [this] [getName] abstract)
  ])

(defun [createEmployee name wage]
  [do
  (def [this] (createPersonBase name))
  (=> [this] [wage] wage)
  ])

(def [tamara] (createEmployee "tamara" 6000))
(assert (call [tamara] [getName] nil) (error "MethodNotImplemented") "Method detected as not implemented")
(printStepResult)


(printStep "Abstract base class fully implemented")
(defun [createPersonBase name]
  [do
  (def [this] [])
  (=> [this] [name] name)
  (=> [this] [getName] abstract)
  ])

(defun [createEmployee name wage]
  [do
  (def [this] (createPersonBase name))
  (=> [this] [wage] wage)
  (=> [this] [getName] (lambda [self dummy] [(-> self [name])]))
  ])

(def [tamara] (createEmployee "tamara" 6000))
(assert (call [tamara] [getName] nil) "tamara" "Method correctly overwritten")
(printStepResult)

(printTestResult)